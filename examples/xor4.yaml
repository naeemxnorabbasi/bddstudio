project:
  name: "4 Input XOR"

signals:
  inputs:
    - a
    - b
    - c
    - d

  intermediates: {}

  outputs:
    y: "a ^ b ^ c ^ d"

bdd:
  variable_order:
    - a
    - b
    - c
    - d

render:
  output_dir: "build/xor4"

  formats:
    dot: true
    png: true
    svg: true
    pdf: true
    markdown: true
    html: true
